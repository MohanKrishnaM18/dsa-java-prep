## Daily Log

**Day 1** — Hibernate/JPA: Setup + JPA Annotations (Section 3, 10/29)
Learned JPA overview, dev environment setup, and DB table setup. Working on connecting the "why" behind JPA annotations before moving further.

**Day 2** — Hibernate/JPA: Saving Java Objects + Primary Keys (Lectures 70-75, 15/29)
Learned how JPA saves a Java object to the DB — annotations like @Id and @GeneratedValue map the object to a table, and Hibernate generates the SQL insert behind the scenes (ORM). Also covered primary keys and MySQL auto-increment indexing.
